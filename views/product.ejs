<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Comic Book App</title>
</head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">Comic Book App</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
        </div>
    </nav>
    
    <div class="container">
        <br>
        <div class="text-center"><h2>Comic Details</h2></div>
        <br>
        <% if (product) { %> <!-- Check if product data exists -->
            <div class="row">
                <div class="col-md-6">
                    <p><b>Comic Name:</b><br> <%= product.productName %></p>
                    <p><b>Written by:</b><br> <%= product.author %></p>
                    <p><b>With Illustrations by:</b><br> <%= product.illustrator %></p>
                    <p><b>Comic Cover:</b><br> <img src="/images/<%= product.image %>" width="200"></p>
                    <p><b>Issue Count:</b><br> <%= product.issues %></p>
                    <p><b>Price:</b><br> $<%= product.price.toFixed(2) %></p> 
                </div>
                <div class="col-md-6">
                    <h3>Synopsis:</h3>
                    <p><%= product.Synopsis %></p>
                </div>
            </div>
        <% } else { %>
            <p class="text-center">No comics found.</p>
        <% } %>
        <div class="text-center">
            <a href="/" class="btn btn-primary mt-3">Back</a>
        </div>
    </div>
    
</body>
</html>
